import React from "react";

const Products = () => {
  return (
    <button
      className="inline-flex justify-center w-auto px-4 py-2 text-sm font-medium text-white bg-black dark:bg-gray-800 rounded-full hover:bg-gray-800 dark:hover:bg-gray-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 transition-colors duration-300"
    >
      Products
    </button>
  );
};

export default Products;